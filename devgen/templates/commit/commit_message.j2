Analyze the following git diff for `{{ group_name }}` and generate a semantic commit message.
Format:
1. Title: `<type>(<scope>): <summary>` (scope is `{{ group_name }}` or `root`).
2. Optional short summary.
3. Bullet points: `New Features`, `Enhancements`, `Bug Fixes`. Use **bold** for section names.
{% if use_emoji %}
4. Use emojis (e.g., 🚀, 🐛).
{% else %}
4. No emojis.
{% endif %}
Output ONLY the message.

Diff for `{{ group_name }}`:
{{ diff_text }}
